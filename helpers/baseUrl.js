// const baseUrl = process.env.NODE_ENV === 'production' ? "https://mezzaneen.com": "http://localhost:3000"
const baseUrl = process.env.NODE_ENV === 'production' ? "https://mini-repo-demo-enjxqaj2u.vercel.app": "http://localhost:3000"

export default baseUrl
